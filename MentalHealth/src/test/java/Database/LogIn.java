package Database;

import static org.junit.Assert.*;

import org.junit.Test;

public class LogIn {

	@Test
	public void test() {
		// JDBC database = new JDBC();
		// assertEquals("Log in for not existing user",
		// database.loginMedicalRecords("jwill01", "1234").getId(), 2);
	}

}
